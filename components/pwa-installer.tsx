"use client"

// This component is intentionally empty to allow default browser PWA install behavior
// The browser will show its native install prompt when PWA criteria are met
export function PWAInstaller() {
  return null
}
